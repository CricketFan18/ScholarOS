# This file intentionally left empty.
# It makes `tests/` a Python package so that pytest can resolve
# imports like `from core.ingestion import ...` correctly when
# running from the repo root with `make test`.